生成一张论文插图风格的系统架构分层图，宽高比 4:3，浅色背景，蓝色/灰色系学术风格。

图中所有文字使用中文。

—— 图层结构，从上到下分为四层 ——

第一层（顶层）：用户程序层
- 一个浅灰色大框，标题"用户程序"
- 内部三个小框并排：sh、semtest、usertests
- 从这三个框伸出箭头向下指向第二层

第二层：YHC 编译器
- 一个浅蓝色大框，标题"YHC 编译器"
- 内部五个小框用从左到右的箭头串联：
  "词法分析" → "语法分析" → "语义检查" → "IR生成" → "代码生成"
- 最后一个框下方伸出一个箭头指向第三层，标注"ELF32 可执行文件"

第三层：YHOS 操作系统
- 一个浅灰色大框，标题"YHOS 操作系统"
- 内部四个框并排排列，每个框内两行字：
  框1：标题"内存管理"，副标题"Sv32 · COW · Buddy"
  框2：标题"进程调度"，副标题"三级 MLFQ"
  框3：标题"进程间通信"，副标题"信号量"
  框4：标题"文件系统"，副标题"日志型 · virtio"
- 每个框底部伸出一个箭头指向第四层

第四层（底层）：RISC-V RV32 硬件平台
- 一个浅蓝色大框，标题"RV32 处理器平台"
- 内部三个小框并排：
  "自研 CPU 核" → "SoC 总线" → "FPGA 实现"
- 从左到右箭头串联

—— 左侧标注 ——
在整个图的左侧，画一条竖排的箭头线，从上到下贯穿四层，旁边标注"编译时"

—— 右侧标注 ——
在整个图的右侧，画一条竖排的箭头线，从上到下贯穿四层，旁边标注"运行时"

—— 底部流程图 ——
在四层架构图的下方（约占据底部 1/6 高度），画一条从左到右的水平流程线，依次排列六个节点，用箭头串联：
"高级语言源程序" → "YHC 编译" → "ELF32 文件" → "OS 加载" → "进程执行" → "CPU 运行"
这条流程线的标题为"从源码到执行的完整链路"，放在流程线上方居中位置。

—— 细节要求 ——
- 框与框之间间隔适中，不拥挤
- 所有文字使用宋体或黑体风格
- 箭头清晰可见，线宽适中
- 整体风格干净、扁平，不要 3D 效果
- 颜色：第一三层浅灰，第二四层浅蓝，交替区分
- 适当圆角，学术感
