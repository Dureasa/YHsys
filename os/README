# YHsys-rv32

This repository provides the **YHsys-RV32** operating-system kernel and userland for **RISC-V 32-bit (RV32GC)**.

It is intended for learning and experimentation around:
- 32-bit address-space behavior
- memory-management behavior under Sv32
- kernel and user-space compatibility on RV32

## Project Notes

The codebase is tuned for a single-core UP runtime and educational kernel experiments.

## Build And Run

Requirements:
- RISC-V GCC/newlib toolchain
- QEMU user emulation for RISC-V 32-bit (the `qemu-system-riscv32` binary)

Then run:

```sh
make qemu
```

## Test

Run user tests in QEMU shell:

```sh
usertests
```

If reporting a bug, include:
- toolchain version
- qemu version
- full log
- minimal reproduction steps

## License And Credits

Core ideas and baseline educational-kernel practices are attributed to their original academic contributors.
